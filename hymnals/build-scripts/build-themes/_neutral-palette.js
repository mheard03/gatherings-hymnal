/*
import NeutralPalette from "./neutral-palette";
import NeutralPaletteLight from "./_neutral-palette-light";
import NeutralPaletteDark from "./_neutral-palette-dark";

NeutralPalette.from = function (hex, whiteJ = 100, blackJ = 0) {
  if (this.whiteJ < this.blackJ) {
    return new NeutralPaletteDark(hex, whiteJ, blackJ);
  }
  return new NeutralPaletteLight(hex, 100, 0);
}

export default NeutralPalette;
*/